import { createSlice, } from "@reduxjs/toolkit";

const initialState = {
    value: {
        notename: String,
    }
}

const note = createSlice({
    name: "note",
    initialState: {},
    reducers: {

    }
});